```
//  functions declarations are newline-sensitive. 
sum :: [u64] -> u64
sum [] = 0
    (x:xs) = + x $ sum xs
    [l] = l
    // as always. the first cases are matched first. you should use more special cases first
```